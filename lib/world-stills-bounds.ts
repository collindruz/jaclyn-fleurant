/**
 * /the-world draggable stills: keep count in a tight band for layout + decode cost
 * (must stay in sync with `getWorldStillsFromColorGroups` in `placeholders.ts`).
 */
export const WORLD_STILLS_MIN = 6;
export const WORLD_STILLS_MAX = 10;
